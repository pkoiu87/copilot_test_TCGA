import pandas as pd\nimport seaborn as sns\nimport matplotlib.pyplot as plt\n\n# 데이터 불러오기\ndata = pd.read_csv('path_to_your_data.csv', index_col=0)  # 데이터 파일 경로 수정 필요\n\n# BRCA subtype으로 데이터 필터링\nbrca_subtypes = ['BRCA_LumA', 'BRCA_Basal', 'BRCA_Her2']\ndeg_data = data[data['subtype'].isin(brca_subtypes)]\n\n# 상위 20개의 DEG 선정\ndeg_genes = deg_data.nlargest(20, 'log2FC')  # log2FC에 따라 정렬 필요\n\n# 히트맵 그리기\nplt.figure(figsize=(10, 8))\nsns.heatmap(deg_genes.loc[:, 'gene1':'geneN'], annot=True, cmap='coolwarm')  # 'gene1':'geneN'은 실제 유전자 열 이름으로 수정 필요\nplt.title('Top 20 DEGs Heatmap for BRCA Subtypes')\nplt.xlabel('Subtypes')\nplt.ylabel('Genes')\nplt.show()\n